日程調整用スクリプトです。

n_daysが選択日数です。
O(o)を5、△(s)を1、X(x)を0と重み付けします。
行が日付、列が人です。

まずn_days日の日付の組み合わせを生成します。

各組み合わせについて、まず人毎に列の最大値を取得します。
その値の合計がその組み合わせのスコアです。

中間結果ではスコア最大の組み合わせを選択します。

中間で得られた組み合わせをさらに絞ります。

得られた組み合わせで日程が合わない人について考えます。
そのような人で全日程における重み合計の大きい人ができるだけ参加できるようにします。
